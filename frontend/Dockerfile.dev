# ✅ [수정] 베이스 이미지를 alpine 버전에서 표준 버전으로 변경
FROM node:20

# 2. 컨테이너 내부에 앱을 저장할 작업 디렉토리 설정
WORKDIR /app

# 3. package.json과 package-lock.json을 먼저 복사
COPY package*.json ./

# 4. 의존성 설치
RUN npm install

# ✅ [추가] Rollup 네이티브 의존성 수동 설치
# npm이 옵셔널 의존성을 제대로 설치하지 못하는 문제를 해결하기 위함입니다.
RUN npm install --save-dev @rollup/rollup-linux-x64-gnu

# 5. 소스 코드를 컨테이너에 복사 (볼륨 마운트를 사용하므로 이 단계는 생략 가능하지만, 명시적으로 포함)
COPY . .

# 6. 컨테이너 실행 시 Vite 개발 서버를 실행하는 명령어
# '--host' 옵션은 컨테이너 외부에서의 접속을 허용합니다.
CMD ["npm", "run", "dev", "--", "--host"]