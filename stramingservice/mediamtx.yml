# API 및 웹 UI 활성화
api: yes

# HLS 스트리밍 활성화
hls: yes

# 로그 레벨 설정
logLevel: info

# --- FastAPI 백엔드와 연동하는 핵심 설정 ---
# 스트림 연결 시도(publish/read)가 있을 때마다 이 URL로 인증을 요청합니다.
externalAuthenticationURL: http://backspringboot:8000/api/internal/auth/stream